defmodule Cowwboy.AccountsTest do
  use Cowwboy.DataCase

  alias Cowwboy.Accounts

  describe "users" do
    alias Cowwboy.Accounts.User

    @valid_attrs %{loses: 42, nick: "some nick", wins: 42, password: "pass123"}
    @update_attrs %{loses: 43, nick: "some updated nick", wins: 43, password: "pass123"}
    @invalid_attrs %{loses: nil, nick: nil, wins: nil, password: "pass12"}

    def user_fixture(attrs \\ %{}) do
      {:ok, user} =
        attrs
        |> Enum.into(@valid_attrs)
        |> Accounts.create_user()

      user
    end

    test "list_users/0 returns all users" do
      user = user_fixture()
      assert Enum.find(Accounts.list_users(), fn(x) -> x.id == user.id end) != nil
    end

    test "get_user!/1 returns the user with given id" do
      user = user_fixture()
      assert Accounts.get_user!(user.id) == user
    end

    test "create_user/1 with valid data creates a user" do
      assert {:ok, %User{} = user} = Accounts.create_user(@valid_attrs)
      assert user.loses == 0
      assert user.nick == "some nick"
      assert user.wins == 0
    end

    test "create_user/1 with invalid data returns error changeset" do
      assert {:error, %Ecto.Changeset{}} = Accounts.create_user(@invalid_attrs)
    end

    test "update_user/2 with valid data updates the user" do
      user = user_fixture()
      assert {:ok, user} = Accounts.update_user(user, @update_attrs)
      assert %User{} = user
      assert user.loses == 43
      assert user.nick == "some updated nick"
      assert user.wins == 43
    end

    test "update_user/2 with invalid data returns error changeset" do
      user = user_fixture()
      assert {:error, %Ecto.Changeset{}} = Accounts.update_user(user, @invalid_attrs)
      assert user == Accounts.get_user!(user.id)
    end

    test "delete_user/1 deletes the user" do
      user = user_fixture()
      assert {:ok, %User{}} = Accounts.delete_user(user)
      assert_raise Ecto.NoResultsError, fn -> Accounts.get_user!(user.id) end
    end

    test "change_user/1 returns a user changeset" do
      user = user_fixture()
      assert %Ecto.Changeset{} = Accounts.change_user(user)
    end
  end
end
